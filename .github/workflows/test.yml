name: Test Injection PoC
on:
  pull_request:
    types: [opened, edited, synchronize]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test injection
        run: |
          teams='${{ toJson(github.event.pull_request.requested_teams) }}'
          echo "Teams: $teams"
